<ul class="lines">
	<li class="trams">
		<strong>Трамваи:</strong>
		<div class="lines-wrapper">
			{{#each lines.trams}}
				<a class="tram" href="#{{../stopcode}}/0/{{this}}" data-line-name="{{this}}">{{this}}</a>
			{{/each}}
		</div>
	</li>
	<li class="buses">
		<strong>Автобуси:</strong>
		<div class="lines-wrapper">
			{{#each lines.buses}}
				<a class="bus" href="#{{../stopcode}}/1/{{this}}" data-line-name="{{this}}">{{this}}</a>
			{{/each}}
		</div>
	</li>
	<li class="trolleys">
		<strong>Тролеи:</strong>
		<div class="lines-wrapper">
			{{#each lines.trolleys}}
				<a class="trolley" href="#{{../stopcode}}/2/{{this}}" data-line-name="{{this}}">{{this}}</a>
			{{/each}}
		</div>
	</li>
	<li class="subway">
		<div class="lines-wrapper">
			<strong>
				<a class="subway" data-line-type="3">Метро</a>
			</strong>
		</div>
	</li>
</ul>
